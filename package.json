{
  "scripts": {
    "test": "vitest",
    "local": "NODE_ENV=local sls offline --stage local",
    "dev": "NODE_ENV=development sls deploy --stage dev",
    "preprod": "NODE_ENV=preprod sls deploy --stage preprod",
    "prod": "NODE_ENV=production sls deploy --stage prod"
  },
  "devDependencies": {
    "aws-sdk": "^2.1692.0",
    "serverless": "^4.4.18",
    "serverless-aws-documentation": "^1.1.0",
    "serverless-offline": "^14.4.0",
    "vi": "^1.0.0"
  }
}
